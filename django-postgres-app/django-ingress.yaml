apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: django-ingress
spec:
  rules:
    - host: devops-dic2-git.com
      http:
        paths:
          - path: /
            backend:
              serviceName: django-app-service
              servicePort: 8001
